
application = {
    content = {
        width = 320,
        height = 480,
        scale = "Letterbox",
        fps = 60,
    },
}

-- this is always the same configuration: application = then content = then width and height and scale and fps